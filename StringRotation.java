boolean isRotation(String s1, String s2){
    //check if length equal and string not empty
    int len = s1.length();
    if(len = s2.length() && len>0){
        String s1s1 = s1+s1
        return isSubstring(s1s1,s2);
    }
    return false;
}